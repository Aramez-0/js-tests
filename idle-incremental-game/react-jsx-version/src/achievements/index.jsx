import { useState } from "react";

function Achievements({color, title}) {
    return (
        <div></div>
    )
}

export default Achievements

// that's where i leave it for now. if you ask me i prefer javascript
// creating elements with is a pain in the ass
// though i can understand how useful keeping track of variables is with react
//  